diff --git a/apps/openmw/mwgui/settingswindow.cpp b/apps/openmw/mwgui/settingswindow.cpp
index 46a2615..cb3a7b0 100644
--- a/apps/openmw/mwgui/settingswindow.cpp
+++ b/apps/openmw/mwgui/settingswindow.cpp
@@ -137,9 +137,9 @@ namespace
             box->setIndexSelected(MyGUI::ITEM_NONE);
     }
 
-    constexpr std::array<int, 5> shadowMapResolutions = { 512, 1024, 2048, 4096, 8192 };
-    constexpr std::array<const char*, 5> shadowMapQualityNames =
-        { "value.balanced", "value.high", "value.very_high", "value.detailed", "value.maximum" };
+    constexpr std::array<int, 2> shadowMapResolutions = { 512, 1024 };
+    constexpr std::array<const char*, 2> shadowMapQualityNames =
+        { "value.balanced", "value.high" };
     constexpr std::array<const char*, 6> shadowPresetNames =
         { "value.disabled", "value.actor", "value.npc", "value.object", "value.terrain", "value.indoor" };
 
@@ -262,26 +262,23 @@ namespace
         return "balanced";
     }
 
-    constexpr std::array<const char*, 6> terrainPresetNames =
-        { "value.minimum", "value.low", "value.balanced", "value.medium", "value.high", "value.ultra" };
-    constexpr std::array<float, 6> terrainLod =
-        { 0.40f, 0.50f, 0.65f, 0.80f, 1.00f, 1.25f };
-    constexpr std::array<int, 6> vertexLod =
-        { -2, -2, -1, -1, 0, 1 };
-    constexpr std::array<int, 6> compositeLevel =
-        { -3, -3, -2, -2, -1, 0 };
-    constexpr std::array<int, 6> compositeResolution =
-        { 1024, 1024, 1024, 2048, 2048, 4096 };
-    constexpr std::array<float, 6> maxCompositeGeometrySize =
-        { 4.f, 4.f, 6.f, 8.f, 12.f, 16.f };
-    constexpr std::array<float, 6> objectPagingMergeFactor =
-        { 100000.f, 75000.f, 50000.f, 30000.f, 15000.f, 8000.f };
-    constexpr std::array<float, 6> objectPagingMinSize =
-        { 1.f, 0.85f, 0.65f, 0.50f, 0.35f, 0.25f };
-
-    constexpr size_t highTerrainPresetIndex = 4;
-    constexpr size_t ultraMaterialQualityIndex = 4;
-    constexpr size_t qualityMaterialQualityIndex = 3;
+    // Android/mobile-safe landscape presets. Medium is the upper bound.
+    constexpr std::array<const char*, 4> terrainPresetNames =
+        { "value.minimum", "value.low", "value.balanced", "value.medium" };
+    constexpr std::array<float, 4> terrainLod =
+        { 0.40f, 0.50f, 0.65f, 0.80f };
+    constexpr std::array<int, 4> vertexLod =
+        { -3, -2, -1, -1 };
+    constexpr std::array<int, 4> compositeLevel =
+        { -3, -3, -2, -2 };
+    constexpr std::array<int, 4> compositeResolution =
+        { 512, 1024, 1024, 2048 };
+    constexpr std::array<float, 4> maxCompositeGeometrySize =
+        { 4.f, 4.f, 6.f, 8.f };
+    constexpr std::array<float, 4> objectPagingMergeFactor =
+        { 80000.f, 75000.f, 50000.f, 30000.f };
+    constexpr std::array<float, 4> objectPagingMinSize =
+        { 1.f, 0.85f, 0.65f, 0.50f };
 
     void applyTerrainPresetSettings(size_t pos)
     {
@@ -523,25 +520,30 @@ namespace MWGui
         // Keep this list in exactly the same order as the TabItem nodes in
         // openmw_settings_window.layout. Tabs are not registered by layout
         // alone: SectionList drives the left-side category navigation.
-        const std::array<const char*, 15> sectionKeys = {
-            "settings.section.interface",
-            "settings.section.hud",
-            "settings.section.controls",
-            "settings.subsection.display",
-            "settings.subsection.quality",
-            "settings.subsection.water",
-            "settings.subsection.lighting",
-            "settings.subsection.pbr",
-            "settings.subsection.hdr",
-            "settings.subsection.bloom",
-            "settings.subsection.effects",
-            "settings.subsection.world",
-            "settings.subsection.shadows",
-            "settings.section.audio",
-            "settings.section.advanced",
-        };
-        for (const char* key : sectionKeys)
-            mSectionList->addItem(arenaText(key));
+        // Android settings deliberately hide both Effects pages and Advanced.
+        // The shared desktop TabItems stay in the layout, so map visible rows to
+        // their real tab indices instead of assuming index equality.
+        const std::array<std::pair<const char*, size_t>, 13> sections = {{
+            {"settings.section.interface", 0},
+            {"settings.section.hud", 1},
+            {"settings.section.controls", 2},
+            {"settings.subsection.display", 3},
+            {"settings.subsection.quality", 4},
+            {"settings.subsection.water", 5},
+            {"settings.subsection.lighting", 6},
+            {"settings.subsection.pbr", 7},
+            {"settings.subsection.hdr", 8},
+            {"settings.subsection.bloom", 9},
+            {"settings.subsection.world", 12},
+            {"settings.subsection.shadows", 13},
+            {"settings.section.audio", 14},
+        }};
+        mSectionTabIndices.clear();
+        for (const auto& section : sections)
+        {
+            mSectionList->addItem(arenaText(section.first));
+            mSectionTabIndices.push_back(section.second);
+        }
         mSectionList->setIndexSelected(0);
         mSettingsTab->setIndexSelected(0);
 
@@ -627,10 +629,9 @@ namespace MWGui
             mTerrainPreset->addItem(arenaText(name));
         mTerrainPreset->eventComboChangePosition += MyGUI::newDelegate(this, &SettingsWindow::onTerrainPresetChanged);
 
-        for (const char* name : materialQualityNames)
-            mMaterialQuality->addItem(arenaText(name));
-        mMaterialQuality->eventComboChangePosition += MyGUI::newDelegate(
-            this, &SettingsWindow::onMaterialQualityChanged);
+        // PBR material quality is launcher-managed on Android and intentionally
+        // absent from the in-game Quality page to avoid live shader rebuilds.
+        mMaterialQuality->setVisible(false);
 
         for (const char* name : landOptimizationModeNames)
             mLandOptimizationMode->addItem(arenaText(name));
@@ -698,7 +699,6 @@ namespace MWGui
         updateNpcBarModeCombo();
         updateQuickLootModeCombo();
         updateTerrainPresetCombo();
-        updateMaterialQualityCombo();
         updateLandOptimizationModeCombo();
         updateLandDistanceControls();
         updateShadowCombos();
@@ -711,16 +711,21 @@ namespace MWGui
 
     void SettingsWindow::onTabChanged(MyGUI::TabControl* /*_sender*/, size_t index)
     {
-        if (index < mSectionList->getItemCount() && mSectionList->getIndexSelected() != index)
-            mSectionList->setIndexSelected(index);
+        const auto it = std::find(mSectionTabIndices.begin(), mSectionTabIndices.end(), index);
+        if (it != mSectionTabIndices.end())
+        {
+            const size_t visibleIndex = static_cast<size_t>(std::distance(mSectionTabIndices.begin(), it));
+            if (mSectionList->getIndexSelected() != visibleIndex)
+                mSectionList->setIndexSelected(visibleIndex);
+        }
         configureWidgets(mMainWidget, false);
         resetScrollbars();
     }
     void SettingsWindow::onSectionSelected(MyGUI::ListBox* /*_sender*/, size_t index)
     {
-        if (index == MyGUI::ITEM_NONE || index >= mSectionList->getItemCount())
+        if (index == MyGUI::ITEM_NONE || index >= mSectionTabIndices.size())
             return;
-        mSettingsTab->setIndexSelected(index);
+        mSettingsTab->setIndexSelected(mSectionTabIndices[index]);
         configureWidgets(mMainWidget, false);
         resetScrollbars();
     }
@@ -1210,32 +1215,12 @@ namespace MWGui
         // their detail/performance balance.
         applyTerrainPresetSettings(pos);
 
-        // Maximum PBR is intentionally tied to High/Ultra terrain. Lowering the
-        // terrain preset automatically falls back to the Quality PBR mode.
-        if (pos < highTerrainPresetIndex && getMaterialQualityMode() == "ultra")
-        {
-            applyMaterialQualitySettings(qualityMaterialQualityIndex);
-            mMaterialQuality->setIndexSelected(qualityMaterialQualityIndex);
-        }
         apply();
     }
 
-    void SettingsWindow::onMaterialQualityChanged(MyGUI::ComboBox*, size_t pos)
+    void SettingsWindow::onMaterialQualityChanged(MyGUI::ComboBox*, size_t)
     {
-        if (pos == MyGUI::ITEM_NONE)
-            return;
-        pos = std::min(pos, materialQualityModes.size() - 1);
-
-        // Selecting Maximum PBR automatically raises landscape detail to High.
-        // This keeps the expensive terrain POM/AO path from running on low-detail land.
-        if (pos == ultraMaterialQualityIndex && getTerrainPresetPosition() < highTerrainPresetIndex)
-        {
-            applyTerrainPresetSettings(highTerrainPresetIndex);
-            mTerrainPreset->setIndexSelected(highTerrainPresetIndex);
-        }
-
-        applyMaterialQualitySettings(pos);
-        apply();
+        // Intentionally inert on Android: material quality is not exposed here.
     }
 
     void SettingsWindow::onLandOptimizationModeChanged(MyGUI::ComboBox*, size_t pos)
@@ -1533,7 +1518,6 @@ namespace MWGui
         updateNpcBarModeCombo();
         updateQuickLootModeCombo();
         updateTerrainPresetCombo();
-        updateMaterialQualityCombo();
         updateLandOptimizationModeCombo();
         updateLandDistanceControls();
         updateShadowCombos();
diff --git a/apps/openmw/mwgui/settingswindow.hpp b/apps/openmw/mwgui/settingswindow.hpp
index 540e313..b0b92fa 100644
--- a/apps/openmw/mwgui/settingswindow.hpp
+++ b/apps/openmw/mwgui/settingswindow.hpp
@@ -24,6 +24,7 @@ namespace MWGui
     protected:
             MyGUI::TabControl* mSettingsTab;
             MyGUI::ListBox* mSectionList;
+            std::vector<size_t> mSectionTabIndices;
             MyGUI::ScrollView* mInterfaceScroll;
             MyGUI::ScrollView* mHudScroll;
             MyGUI::ScrollView* mDisplayScroll;
diff --git a/apps/openmw/mwrender/nativeeffects.cpp b/apps/openmw/mwrender/nativeeffects.cpp
index 2bc4ac9..9908d0b 100644
--- a/apps/openmw/mwrender/nativeeffects.cpp
+++ b/apps/openmw/mwrender/nativeeffects.cpp
@@ -371,9 +371,9 @@ namespace MWRender
 
     void NativeEffectsProcessor::captureWorldDepthBeforeFirstPersonClear(osg::RenderInfo& renderInfo)
     {
-        NativeEffectsProcessor* processor = sActiveProcessor.load(std::memory_order_acquire);
-        if (processor)
-            processor->captureWorldDepth(renderInfo);
+        // Disabled on the Android compatibility renderer. Copying world depth
+        // before the first-person clear can produce a black frame on GLES/gl4es.
+        (void)renderInfo;
     }
 
     void NativeEffectsProcessor::reloadSettings()
@@ -383,8 +383,10 @@ namespace MWRender
 
         mSmaaEnabled = Settings::Manager::getBool("smaa enabled", "Shaders");
         mBloomEnabled = Settings::Manager::getBool("bloom enabled", "Shaders");
-        mAtmosphericFogEnabled = Settings::Manager::getBool("atmospheric fog enabled", "Shaders");
-        mGodRaysEnabled = Settings::Manager::getBool("god rays enabled", "Shaders");
+        // Hard-disable the native depth-dependent effects on Android builder
+        // profiles. Stale settings.cfg values must never reactivate them.
+        mAtmosphericFogEnabled = false;
+        mGodRaysEnabled = false;
         mSharpeningEnabled = Settings::Manager::getBool("sharpening enabled", "Shaders");
         mDitheringEnabled = Settings::Manager::getBool("dithering enabled", "Shaders");
 
@@ -420,7 +422,7 @@ namespace MWRender
 
         Log(Debug::Info) << "ArenaMW native effects: fog=" << (mAtmosphericFogEnabled ? "on" : "off")
                          << ", godRays=" << (mGodRaysEnabled ? "on" : "off")
-                         << ", firstPersonWorldDepthBridge=on";
+                         << ", firstPersonWorldDepthBridge=off";
     }
 
     void NativeEffectsProcessor::setEnvironment(const osg::Vec4f& fogColor, float fogStart, float fogEnd,
diff --git a/files/mygui/openmw_settings_window.layout b/files/mygui/openmw_settings_window.layout
index 2fb8077..5acb51a 100644
--- a/files/mygui/openmw_settings_window.layout
+++ b/files/mygui/openmw_settings_window.layout
@@ -361,21 +361,6 @@
                 <Property key="Caption" value="#{arenamp=settings.display.smaa_enabled}"/>
               </Widget>
             </Widget>
-            <Widget type="TextBox" skin="NormalText" position="244 132 206 18" name="SmaaThresholdLabel">
-              <Property key="Caption" value="#{arenamp=settings.display.smaa_threshold}"/>
-            </Widget>
-            <Widget type="ScrollBar" skin="MW_HScroll" position="244 152 206 16">
-              <Property key="Range" value="101"/>
-              <Property key="Page" value="1"/>
-              <UserString key="SettingType" value="Slider"/>
-              <UserString key="SettingCategory" value="Shaders"/>
-              <UserString key="SettingName" value="smaa threshold"/>
-              <UserString key="SettingValueType" value="Float"/>
-              <UserString key="SettingMin" value="0.03"/>
-              <UserString key="SettingMax" value="0.30"/>
-              <UserString key="SettingLabelWidget" value="SmaaThresholdLabel"/>
-              <UserString key="SettingLabelCaption" value="#{arenamp=settings.display.smaa_threshold}"/>
-            </Widget>
             <Widget type="TextBox" skin="NormalText" position="8 236 442 18" name="FovText">
               <Property key="Caption" value="#{arenamp=settings.fov}"/>
             </Widget>
@@ -501,37 +486,34 @@
             <Property key="Caption" value="#{arenamp=settings.terrain_detail}"/>
           </Widget>
           <Widget type="ComboBox" skin="MW_ComboBox" position="8 30 260 24" name="TerrainPreset"/>
-          <Widget type="TextBox" skin="NormalText" position="8 66 462 18">
-            <Property key="Caption" value="#{arenamp=settings.material_quality}"/>
-            <UserString key="ToolTipType" value="Layout"/>
-            <UserString key="ToolTipLayout" value="TextToolTip"/>
-            <UserString key="Caption_Text" value="#{arenamp=settings.tooltip.material_quality}"/>
+          <!-- Android: PBR quality selector removed from the Quality page. -->
+          <Widget type="ComboBox" skin="MW_ComboBox" position="0 0 1 1" name="MaterialQuality">
+            <Property key="Visible" value="false"/>
           </Widget>
-          <Widget type="ComboBox" skin="MW_ComboBox" position="8 88 260 24" name="MaterialQuality"/>
-          <Widget type="TextBox" skin="NormalText" position="8 124 462 18">
+          <Widget type="TextBox" skin="NormalText" position="8 66 462 18">
             <Property key="Caption" value="#{arenamp=settings.texture_filtering}"/>
           </Widget>
-          <Widget type="ComboBox" skin="MW_ComboBox" position="8 146 260 24" name="TextureFilteringButton"/>
-          <Widget type="TextBox" skin="NormalText" position="8 182 462 18">
+          <Widget type="ComboBox" skin="MW_ComboBox" position="8 88 260 24" name="TextureFilteringButton"/>
+          <Widget type="TextBox" skin="NormalText" position="8 124 462 18">
             <Property key="Caption" value="#{arenamp=settings.land_optimization}"/>
             <UserString key="ToolTipType" value="Layout"/>
             <UserString key="ToolTipLayout" value="TextToolTip"/>
             <UserString key="Caption_Text" value="#{arenamp=settings.tooltip.land_optimization}"/>
           </Widget>
-          <Widget type="ComboBox" skin="MW_ComboBox" position="8 204 260 24" name="LandOptimizationMode"/>
-          <Widget type="Widget" skin="" position="8 242 462 70" name="ManualRenderDistanceBox">
+          <Widget type="ComboBox" skin="MW_ComboBox" position="8 146 260 24" name="LandOptimizationMode"/>
+          <Widget type="Widget" skin="" position="8 184 462 70" name="ManualRenderDistanceBox">
             <Widget type="TextBox" skin="NormalText" position="0 6 462 18" name="RenderDistanceLabel">
               <Property key="Caption" value="#{sRender_Distance}"/>
             </Widget>
             <Widget type="ScrollBar" skin="MW_HScroll" position="0 28 462 16" name="LargeRenderingDistanceSlider">
-              <Property key="Range" value="79421"/>
-              <Property key="Page" value="2048"/>
+              <Property key="Range" value="5693"/>
+              <Property key="Page" value="256"/>
               <UserString key="SettingType" value="Slider"/>
               <UserString key="SettingCategory" value="Camera"/>
               <UserString key="SettingName" value="viewing distance"/>
               <UserString key="SettingValueType" value="Cell"/>
               <UserString key="SettingMin" value="2500"/>
-              <UserString key="SettingMax" value="81920"/>
+              <UserString key="SettingMax" value="8192"/>
               <UserString key="SettingLabelWidget" value="RenderDistanceLabel"/>
               <UserString key="SettingLabelCaption" value="#{sRender_Distance} (x%s)"/>
             </Widget>
diff --git a/files/settings-default.cfg b/files/settings-default.cfg
index 229780b..ba924f6 100644
--- a/files/settings-default.cfg
+++ b/files/settings-default.cfg
@@ -397,9 +397,9 @@ smaa threshold = 0.10
 
 # Native Luxora-inspired post effects. All run before MyGUI so HUD/menu text
 # stays untouched. SSAO and Photon volumetric clouds are intentionally excluded.
-atmospheric fog enabled = true
+atmospheric fog enabled = false
 atmospheric fog strength = 0.28
-god rays enabled = true
+god rays enabled = false
 god rays strength = 0.65
 sharpening enabled = true
 sharpening strength = 0.32
