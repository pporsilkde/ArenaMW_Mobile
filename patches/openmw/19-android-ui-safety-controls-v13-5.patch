diff --git a/apps/openmw/mwgui/settingswindow.cpp b/apps/openmw/mwgui/settingswindow.cpp
index dc4113f..5cb22d2 100644
--- a/apps/openmw/mwgui/settingswindow.cpp
+++ b/apps/openmw/mwgui/settingswindow.cpp
@@ -515,7 +515,11 @@ namespace MWGui
         // Keep this list in exactly the same order as the Android TabItem nodes in
         // openmw_settings_window.layout. PBR, HDR, Bloom, Effects and Advanced are
         // removed from the Android layout; the launcher owns the stable graphics presets.
-        const std::array<std::pair<const char*, size_t>, 10> sections = {{
+        // Shadows stay in the layout so the existing widget wiring remains valid,
+        // but the section is intentionally not exposed on Android. Live shadow
+        // reconfiguration is unsafe on the mobile renderer and can crash the client;
+        // launcher presets apply shadow values before the renderer starts instead.
+        const std::array<std::pair<const char*, size_t>, 9> sections = {{
             {"settings.section.interface", 0},
             {"settings.section.hud", 1},
             {"settings.section.controls", 2},
@@ -524,7 +528,6 @@ namespace MWGui
             {"settings.subsection.water", 5},
             {"settings.subsection.lighting", 6},
             {"settings.subsection.world", 7},
-            {"settings.subsection.shadows", 8},
             {"settings.section.audio", 9},
         }};
         mSectionTabIndices.clear();
diff --git a/files/mygui/openmw_hud.layout b/files/mygui/openmw_hud.layout
index f2a49f6..3c71fda 100644
--- a/files/mygui/openmw_hud.layout
+++ b/files/mygui/openmw_hud.layout
@@ -6,7 +6,7 @@
         <Widget type="Widget" position="0 0 300 200" name="GameplayHud" align="Stretch">
             <Property key="NeedMouse" value="false"/>
 	
-        <Widget type="TextBox" skin="SandBrightText" position="30 6 72 22" name="FpsText" align="Left Top">
+        <Widget type="TextBox" skin="SandBrightText" position="96 6 72 22" name="FpsText" align="Left Top">
             <Property key="NeedMouse" value="false"/>
             <Property key="TextAlign" value="Left"/>
             <Property key="TextShadow" value="true"/>
