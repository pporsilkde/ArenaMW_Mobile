diff --git a/apps/openmw/mwgui/settingswindow.cpp b/apps/openmw/mwgui/settingswindow.cpp
index 5cb22d2..505f48a 100644
--- a/apps/openmw/mwgui/settingswindow.cpp
+++ b/apps/openmw/mwgui/settingswindow.cpp
@@ -210,10 +210,10 @@ namespace
         return "balance";
     }
 
-    constexpr std::array<const char*, 3> waterShaderModeNames =
-        { "value.off", "value.water_simple", "value.water_new" };
-    constexpr std::array<const char*, 3> waterShaderModes =
-        { "off", "simple", "new" };
+    constexpr std::array<const char*, 2> waterShaderModeNames =
+        { "value.off", "value.water_simple" };
+    constexpr std::array<const char*, 2> waterShaderModes =
+        { "off", "simple" };
 
     std::string getWaterShaderMode()
     {
@@ -230,9 +230,9 @@ namespace
             }
         }
 
-        // Migrate old profiles: shader=false was classic animated water,
-        // shader=true was the current ArenaMW PBR water.
-        return Settings::Manager::getBool("shader", "Water") ? "new" : "off";
+        // Migrate old profiles onto the stable mobile water path. Older
+        // shader=true profiles used PBR water; on Android they now map to simple.
+        return Settings::Manager::getBool("shader", "Water") ? "simple" : "off";
     }
 
     constexpr std::array<const char*, 5> materialQualityNames =
@@ -1384,7 +1384,7 @@ namespace MWGui
                 return;
             }
         }
-        mWaterShaderMode->setIndexSelected(2);
+        mWaterShaderMode->setIndexSelected(1);
     }
 
     void SettingsWindow::selectLightingMethod(const std::string& value)
diff --git a/apps/openmw/mwrender/renderingmanager.cpp b/apps/openmw/mwrender/renderingmanager.cpp
index 44d0345..6ab3bc4 100644
--- a/apps/openmw/mwrender/renderingmanager.cpp
+++ b/apps/openmw/mwrender/renderingmanager.cpp
@@ -102,7 +102,6 @@ namespace
     constexpr float sLandOptimizationDistanceResponse = 4.f;
     constexpr float sLandOptimizationSmoothingMaxFrameTime = 0.1f;
     constexpr float sLandOptimizationMinimumScale = 0.5f;
-    constexpr float sLandOptimizationShadowDistanceRatio = 0.25f;
     constexpr float sLandOptimizationGroundcoverDistanceRatio = 0.40f;
 
     int getMaterialQualityLevel()
@@ -1775,12 +1774,13 @@ namespace MWRender
 
         if (mShadowManager)
         {
-            const float configuredShadowDistance = mLandOptimizationEnabled
-                ? mConfiguredViewDistance * sLandOptimizationShadowDistanceRatio
-                : Settings::Manager::getFloat("maximum shadow map distance", "Shadows");
-            const float shadowDistance = std::clamp(configuredShadowDistance, 0.f, 8192.f);
-            mShadowManager->setMaximumShadowMapDistance(
-                std::clamp(shadowDistance * adaptiveDistanceScale, 0.f, 8192.f));
+            // Shadow distance is an explicit user setting. Land optimization must not
+            // replace 8192 with 25% of world distance or apply adaptive scaling again.
+            // Keep this independent from dynamic terrain distance; the mobile hard cap
+            // is already 8192, so the selected value remains predictable and safe.
+            const float configuredShadowDistance = std::clamp(
+                Settings::Manager::getFloat("maximum shadow map distance", "Shadows"), 0.f, 8192.f);
+            mShadowManager->setMaximumShadowMapDistance(configuredShadowDistance);
         }
     }
 
diff --git a/apps/openmw/mwrender/water.cpp b/apps/openmw/mwrender/water.cpp
index afe7afc..d1cfd60 100644
--- a/apps/openmw/mwrender/water.cpp
+++ b/apps/openmw/mwrender/water.cpp
@@ -623,7 +623,11 @@ void Water::updateWaterMaterial()
     std::string shaderWaterMode = Settings::Manager::getString("shader mode", "Water");
     Misc::StringUtils::lowerCaseInPlace(shaderWaterMode);
     if (shaderWaterMode != "off" && shaderWaterMode != "simple" && shaderWaterMode != "new")
-        shaderWaterMode = Settings::Manager::getBool("shader", "Water") ? "new" : "off";
+        shaderWaterMode = Settings::Manager::getBool("shader", "Water") ? "simple" : "off";
+    // V13.7.4 mobile safety: old configs may still explicitly contain "new".
+    // Keep PBR code available in source, but never select it in the Android build.
+    if (shaderWaterMode == "new")
+        shaderWaterMode = "simple";
 
     const bool shaderWaterEnabled = shaderWaterMode != "off";
     const bool pbrWaterEnabled = shaderWaterMode == "new";
diff --git a/files/settings-default.cfg b/files/settings-default.cfg
index 124cdc1..4f3b777 100644
--- a/files/settings-default.cfg
+++ b/files/settings-default.cfg
@@ -510,13 +510,14 @@ object display lists = true
 
 [Water]
 # Water rendering mode: off, simple, new.
-# "simple" uses ArenaMW legacy water shaders; "new" uses the current PBR water.
-shader mode = new
+# Android/mobile default: use the stable simple shader-water path.
+# The PBR/new path remains in source for future work but is not exposed by this build.
+shader mode = simple
 # Legacy compatibility key kept in sync by the in-game selector.
 shader = true
-rtt size = 512
-refraction = true
-reflection detail = 3
+rtt size = 256
+refraction = false
+reflection detail = 2
 
 # Runtime caustics multiplier. Valid UI range: 0.0 to 3.0.
 caustics intensity = 1.0
