# V13.7.7

- Replaced all launcher app icons (main/mainline/nightly) with the user-provided ArenaMW emblem image.
- Replaced the launcher start FAB icon `ic_start_button.png` with the same user-provided image.
- Updated `ic_launcher.png`, `ic_launcher_round.png`, and `ic_launcher_foreground.png` in all density buckets.
